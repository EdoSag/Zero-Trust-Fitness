name: zerotrust_fitness
description: A new Nowa project.
publish_to: none
version: 0.0.1+1
environment:
  sdk: ">=3.2.0 <4.0.0"
dependencies:
  flutter:
    sdk: flutter
  nowa_runtime: ^0.1.3
  provider: ^6.1.5
  shared_preferences: ^2.5.3
  dio: ^5.5.0+1
  go_router: ^17.0.1
  supabase_flutter: ^2.9.0
  riverpod: latest
  riverpod_annotation: latest
  cryptography_flutter: latest
  sqlcipher_flutter_libs: latest
  drift: latest
  health: latest
  cryptography: latest
  flutter_secure_storage: latest
  flutter_dotenv: latest
  workmanager: latest
  home_widget: latest
  fl_chart: latest
  shimmer: latest
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
flutter:
  uses-material-design: true
  assets:
    - assets/
